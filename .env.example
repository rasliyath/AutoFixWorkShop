# Example environment variables for local development and deployment

# Flask backend URL used by frontend and agents (set to deployed backend URL in production)
VITE_API_URL=http://localhost:5001
FLASK_URL=http://localhost:5001

# Mongo, LiveKit and other keys are omitted here. Keep secrets in your deployment environment or a secure secrets store.
